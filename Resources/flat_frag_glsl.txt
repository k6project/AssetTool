#version 400

layout(location=0) in lowp vec3 FragNormal;
layout(location=1) in lowp vec3 FragCoord;

layout(location=0) out highp vec4 FragColor;

uniform highp vec4 FillColor;

void main()
{
    FragColor = vec4(FragCoord, 1.0);
}
