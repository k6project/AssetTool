#version 400

uniform mat4 MVP;

layout(location=0) in vec3 Position;
layout(location=1) in vec3 Normal;

out vec3 FragNormal;
out vec3 FragCoord;

void main()
{
    FragNormal = Normal;
    FragCoord = Position;
    gl_Position = MVP * vec4(Position, 1.0);
}
